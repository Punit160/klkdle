# DLE

## File storage

- **Existing modules** (AMC, profile, light AMC): local disk — see `UPLOADS_DIR` in `Backend/.env.example`.
- **New modules**: Cloudflare R2 bucket `klkdle` — full setup in [docs/object-storage.md](docs/object-storage.md).
